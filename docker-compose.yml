version: '3.8'

services:
  bds:
    image: itzg/minecraft-bedrock-server:2025.7.0
    environment:
      EULA: "TRUE"
      VERSION: '1.21.95.1'
    ports:
      - "19132:19132/udp"
    volumes:
      - mc-bedrock-data:/data
    stdin_open: true
    tty: true

  unmined-cli:
    build:
      context: .
      dockerfile: unmined-cli/Dockerfile
    volumes:
      - mc-bedrock-data:/data

volumes:
  mc-bedrock-data:
